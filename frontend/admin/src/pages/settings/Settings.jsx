import { useState } from "react";import { Save } from "lucide-react";import AdminModuleShell from "../../components/AdminModuleShell";
export default function Settings(){const[form,setForm]=useState({businessName:"ASH Jewellery",currency:"INR",enquiryChannel:"WhatsApp",whatsappNumber:"",storefrontStatus:"Live"});const[saved,setSaved]=useState(false);const submit=e=>{e.preventDefault();localStorage.setItem("ashSettings",JSON.stringify(form));setSaved(true);setTimeout(()=>setSaved(false),1500)};return <AdminModuleShell eyebrow="MANAGEMENT / SETTINGS" title="Settings" description="Manage core business, enquiry and storefront configuration."><div className="products-card module-card"><div className="products-card-header"><div><h2>Business settings</h2><p>These controls will map to the production configuration API later.</p></div></div><form className="settings-form" onSubmit={submit}><div className="settings-grid">{Object.entries(form).map(([key,value])=><label className="form-field" key={key}><span>{key.replace(/([A-Z])/g," $1")}</span><input value={value} onChange={e=>setForm({...form,[key]:e.target.value})}/></label>)}</div><button className="save-product-btn" type="submit"><Save size={15}/> Save Settings</button>{saved&&<span className="settings-saved">Saved</span>}</form></div></AdminModuleShell>}
