import { useEffect,useState } from "react";import { useNavigate,useParams } from "react-router-dom";import { ArrowLeft,Save } from "lucide-react";
export default function EditCollections(){const{id}=useParams();const nav=useNavigate();const[item,setItem]=useState(null);useEffect(()=>{const a=JSON.parse(localStorage.getItem("ashCollections")||"[]");setItem(a.find(x=>x.id===id)||null)},[id]);if(!item)return <div className="products-empty"><h3>Collection not found</h3><button className="add-product-btn" onClick={()=>nav("/collections")}>Back</button></div>;const saveIt=e=>{e.preventDefault();const a=JSON.parse(localStorage.getItem("ashCollections")||"[]").map(x=>x.id===id?item:x);localStorage.setItem("ashCollections",JSON.stringify(a));nav("/collections")};return <section className="product-form-page"><div className="product-form-heading"><button className="back-link" onClick={()=>nav("/collections")}><ArrowLeft size={15}/> Back to Collections</button><div className="eyebrow">CATALOGUE / COLLECTIONS</div><h1>Edit Collection</h1><p>Update this catalogue record.</p></div><form className="product-form-layout" onSubmit={saveIt}><main className="product-form-main"><div className="product-form-card"><div className="product-form-card-header"><h2>Basic Information</h2></div><div className="product-form-grid"><label className="form-field full-width"><span>Name *</span><input required value={item.name} onChange={e=>setItem({...item,name:e.target.value})}/></label><label className="form-field full-width"><span>Slug</span><input value={item.slug} onChange={e=>setItem({...item,slug:e.target.value})}/></label><label className="form-field full-width"><span>Description</span><textarea rows="5" value={item.description||""} onChange={e=>setItem({...item,description:e.target.value})}/></label></div></div></main><aside className="product-form-sidebar"><div className="product-form-card"><div className="product-form-card-header"><h2>Publishing</h2></div><label className="publish-option"><input type="checkbox" checked={item.status==="Active"} onChange={e=>setItem({...item,status:e.target.checked?"Active":"Inactive"})}/><span>Active</span></label><button className="save-product-btn" type="submit"><Save size={15}/> Save Changes</button></div></aside></form></section>}
