import { useState } from "react";import { useNavigate } from "react-router-dom";import { ArrowLeft,Save } from "lucide-react";
export default function AddCollections(){const nav=useNavigate();const[name,setName]=useState("");const[description,setDescription]=useState("");const submit=e=>{e.preventDefault();const key="ashCollections";const items=JSON.parse(localStorage.getItem(key)||"[]");items.push({id:"collections-"+Date.now(),name,slug:name.toLowerCase().replace(/[^a-z0-9]+/g,"-"),description,status:"Active",order:items.length+1});localStorage.setItem(key,JSON.stringify(items));nav("/collections")};return <section className="product-form-page"><div className="product-form-heading"><div><button className="back-link" onClick={()=>nav("/collections")}><ArrowLeft size={15}/> Back to Collections</button><div className="eyebrow">CATALOGUE / COLLECTIONS</div><h1>Add Collection</h1><p>Create a new collection record.</p></div></div><form className="product-form-layout" onSubmit={submit}><main className="product-form-main"><div className="product-form-card"><div className="product-form-card-header"><h2>Basic Information</h2><p>Keep the catalogue information clear and customer-ready.</p></div><div className="product-form-grid"><label className="form-field full-width"><span>Name *</span><input required value={name} onChange={e=>setName(e.target.value)} placeholder="Enter name"/></label><label className="form-field full-width"><span>Description</span><textarea rows="5" value={description} onChange={e=>setDescription(e.target.value)} placeholder="Enter description"/></label></div></div></main><aside className="product-form-sidebar"><div className="product-form-card"><div className="product-form-card-header"><h2>Publishing</h2></div><label className="publish-option"><input type="radio" checked readOnly/><span>Active</span></label><button className="save-product-btn" type="submit"><Save size={15}/> Save Collection</button><button type="button" className="cancel-product-btn" onClick={()=>nav("/collections")}>Cancel</button></div></aside></form></section>}
